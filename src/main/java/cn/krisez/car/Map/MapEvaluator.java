package cn.krisez.car.Map;

import android.animation.TypeEvaluator;

import com.amap.api.maps.model.MarkerOptions;

public class MapEvaluator implements TypeEvaluator<MarkerOptions> {
    @Override
    public MarkerOptions evaluate(float fraction, MarkerOptions startValue, MarkerOptions endValue) {
        return null;
    }
}
